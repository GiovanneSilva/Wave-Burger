# CLAUDE.md — Wave Burger

> Este arquivo é a instrução permanente para qualquer agente (Claude ou outro) que trabalhe neste repositório. Ele deve ser lido antes de qualquer implementação. Em caso de conflito entre este arquivo e um pedido pontual, sinalize o conflito antes de agir.

## 1. Objetivo do Wave Burger

O Wave Burger é uma plataforma de gestão inteligente para uma hamburgueria que nasce em operação de **delivery**, inicialmente via iFood, com possibilidade de expansão futura para múltiplas unidades e franquias.

O produto **não é um sistema de cadastro**. Seu propósito é centralizar dados operacionais, financeiros e comerciais e transformá-los em respostas para perguntas de negócio (custo real de produção, margem por produto, impacto de aumento de custo de insumo, ponto de equilíbrio, etc.).

Fonte da verdade do negócio: **`Wave Burger — Documento Mestre de Produto e Negócio` (v0.1)**. Este `CLAUDE.md` não substitui o Documento Mestre — ele traduz a documentação em regras de engenharia.

## 2. Módulos

Na ordem em que aparecem no Documento Mestre:

1. Produtos
2. Ficha Técnica (núcleo funcional do sistema)
3. Ingredientes
4. Fornecedores
5. Compras
6. Estoque
7. Financeiro
8. BI e Analytics
9. Usuários, Perfis e Permissões
10. Auditoria

Módulos adicionais tratados pelo roadmap de implementação, mas ainda sem especificação formal completa no Documento Mestre (ver `docs/pendencias.md`):

- **Vendas/Pedidos manuais** — escopo confirmado com o usuário na Etapa 16 (ver `pendencias.md`, PD-010 resolvida): venda de produto único (produto + quantidade + preço + data) com desconto simples opcional. Implementado.
- **Integração iFood** — citada na Visão, Premissas e Roadmap, mas o "capítulo de integração" ainda não existe no Documento Mestre. Não deve ser implementada sem essa definição prévia (Etapa 20).

## 3. Arquitetura

**Stack técnica definitiva (Etapa 3 — confirmada em 17/08/2026):**

- **Frontend:** Next.js + TypeScript
- **Backend:** NestJS + TypeScript
- **Banco de dados:** PostgreSQL
- **ORM:** Prisma
- **Testes backend:** Jest
- **Testes end-to-end:** Playwright
- **Ambiente local:** Docker

Justificativa (avaliada contra simplicidade, manutenção, geração de código com IA, integração futura com iFood e crescimento multi-unidade):

- TypeScript ponta a ponta reduz erro de contrato entre frontend e backend, e é a combinação mais bem representada em geração de código por IA, reduzindo risco de alucinação de API.
- NestJS oferece módulos com injeção de dependência que mapeiam diretamente para a exigência de modularidade e isolamento de integrações externas (Seção 1.7/8 do Documento Mestre) — a futura integração com iFood poderá ser implementada como módulo/adapter isolado sem vazar seu formato para o domínio.
- PostgreSQL + Prisma suportam nativamente tipo `NUMERIC`/`Decimal`, atendendo à regra de nunca usar `float`/`double` para valores monetários (Seção 5 deste documento).
- PostgreSQL lida bem com o modelo relacional multi-tenant (`Organization`/`BusinessUnit`) exigido desde a fundação do banco (Seção 3, princípio "multi-unidade desde o início").
- Prisma facilita migrations versionadas e reversíveis, alinhado à Seção 6 e 9 deste documento.

**Convenção obrigatória decorrente da stack:** todo valor monetário ou de custo deve usar o tipo `Decimal` do Prisma/PostgreSQL (nunca `number`/`float` em TypeScript) em qualquer camada — modelo, DTO, cálculo ou serialização.

Princípios arquiteturais já válidos independentemente da stack escolhida (derivados da Seção 1.7 e 8 do Documento Mestre):

- **Fonte única da verdade**: uma informação fundamental tem um único cadastro central. Exemplo: o custo do ingrediente existe apenas no cadastro do ingrediente (BR-003); nunca duplicar esse dado em outra entidade.
- **Automação de cálculos**: sempre que o sistema tiver os dados necessários, o cálculo não deve depender de intervenção humana.
- **Modularidade**: módulos devem poder evoluir de forma independente, mas sem duplicar regras de negócio entre si. Efeitos entre módulos (ex.: compra confirmada → estoque → financeiro) devem ser implementados via serviços de aplicação ou eventos internos, nunca duplicando lógica em cada módulo.
- **Multi-unidade desde o início**: as entidades `Organization` e `BusinessUnit` devem existir desde a fundação do banco, mesmo que o MVP opere com uma única unidade. Não construir premissas de "unidade única" nos modelos de domínio.
- **Isolamento de integrações externas**: qualquer integração externa (iFood e futuras) deve ser implementada como adapter isolado. O domínio do Wave Burger nunca deve depender diretamente do formato de uma API externa.
- **IDs**: todas as entidades de negócio usam UUID como identificador.
- **Simplicidade no MVP**: não implementar complexidade de franquia, multi-unidade operacional ou integrações automáticas enquanto não solicitado.

## 4. Regras arquiteturais

- Nenhum módulo de negócio implementa auditoria própria — todos reutilizam o mecanismo central de `AuditLog` (Etapa 7 do roadmap).
- Nenhum módulo de negócio implementa checagem de permissão própria — toda operação sensível passa pelo mecanismo central de autorização baseado em `Role`/`Permission`.
- Registros historicamente relevantes (ex.: fichas técnicas, movimentações de estoque, log de auditoria) são **imutáveis/append-only**; correções são feitas por novos registros, nunca por edição destrutiva do histórico.
- Entidades com impacto operacional (Produto, Ingrediente, Fornecedor) são inativadas, nunca excluídas fisicamente, preservando histórico (RF-003).
- Toda mudança de estado com efeito em múltiplos módulos (ex.: confirmação de compra) deve ser transacional — falha parcial não pode deixar estoque, financeiro ou auditoria inconsistentes entre si.

## 5. Convenções de código

A serem detalhadas junto da definição de stack (Etapa 3). Já valem, independentemente da stack:

- Nomenclatura das entidades no código deve espelhar os termos do Modelo de Domínio do Documento Mestre (`Produto`, `FichaTecnica`, `Ingrediente`, `Fornecedor`, `Compra`, `Estoque`, `MovimentacaoEstoque`, `Venda`, `LancamentoFinanceiro`, `Usuario`, `Role`, `Permission`, `AuditLog`) para manter rastreabilidade entre documentação e código.
- Valores monetários e de custo nunca usam ponto flutuante (`float`/`double`); usar tipo decimal/fixed-point para evitar erro de arredondamento em cálculos de CMV, markup e margem.
- Datas/horas armazenadas em UTC; conversão de fuso tratada na camada de apresentação.

## 5.1. Frontend — Design System v1

Direção visual definida e aprovada pelo usuário em 19/08/2026, a partir de um briefing próprio (referência: Linear + Stripe Dashboard + shadcn dashboard, com personalidade da marca Wave Burger — "empresarial, atual, rápido, com um toque de hamburgueria só na identidade, nunca no sistema inteiro").

**Stack:** Next.js (App Router) + Tailwind CSS **v3.4.19** (fixado deliberadamente — `npm install` sem versão pinada puxa a v4, que ainda não tem compatibilidade madura com o ecossistema shadcn/Radix usado aqui) + shadcn/ui (componentes escritos à mão no padrão Radix+CVA, sem depender do CLI do shadcn) + Lucide Icons + Inter (via `next/font/google`).

**Paleta** (extraída do logo real da marca, convertida para HSL com precisão — ver `app/globals.css`):
| Token | Hex | Uso |
|---|---|---|
| `brand-primary` | `#0B6FB0` | Ações primárias, links ativos, accent geral |
| `sidebar-bg` | `#12213D` | Fundo da sidebar (azul-marinho do logo) |
| `success` | `#16A34A` | Estoque ok, margem saudável |
| `warning` | `#D97706` | Atenção (estoque baixo, margem caindo) |
| `danger` | `#D62828` | Erro/crítico — mesmo vermelho do script "Lanches" do logo, não inventado |
| `accent-warm` | `#F5821F` | Uso pontual, nunca dominante |

**Estrutura de navegação** (aprovada, não inventar itens novos sem sinalizar): Dashboard (solto) → Operação (Produtos, Fichas técnicas, Ingredientes, Estoque, Compras, Fornecedores) → Gestão (Financeiro, BI/Indicadores) → Sistema (Usuários, Auditoria, Configurações).

**Componentes de domínio obrigatórios** (`frontend/components/wave/`) — não recriar variantes ad-hoc por tela: `KpiCard` (clicável, padrão indicador→detalhe→causa→ação), `StatusBadge`, `DataTable`, `MoneyValue`, `PercentageChange`, `StockStatus` (3 estados: normal/atenção/crítico, nunca pinta a linha inteira), `EmptyState` (convite, não desculpa — segue o guia de copy do `frontend-design` skill), `ConfirmDialog` (só para ação irreversível/de impacto real, não "modal para tudo"), `PageHeader`, `AuditTimeline` (consome o `AuditLog` do backend, Etapa 7, sem reimplementar lógica de histórico).

**Evitar deliberadamente** (lista do próprio briefing aprovado): glassmorphism, gradientes, cards dentro de cards, sombras fortes, mais de uma cor principal disputando atenção, dashboard cheio de gráfico, modal para tudo.

**Limitação de sandbox conhecida:** `next/font/google` (Inter) não builda neste ambiente porque `fonts.googleapis.com` não está na allowlist de rede — mesma classe de limitação já documentada para o Prisma (Etapa 5) e para instalação de browser do Playwright (Etapa 4). Validado contornando temporariamente a fonte só para confirmar que o resto do build/lint/tsc está limpo; a configuração real do Inter foi restaurada antes do commit e funciona normalmente com internet completa.

**Autenticação (padrão obrigatório para toda tela nova):** o token JWT fica em cookie `httpOnly` (`wb_session`), nunca em `localStorage`, nunca exposto ao JavaScript do navegador — reduz superfície de roubo via XSS. Toda tela que precisa de dado real do backend deve seguir um destes dois caminhos, nunca fazer `fetch` direto do cliente com o token:
1. Route Handler em `app/api/**` lê o cookie no servidor (`request.cookies.get('wb_session')`) e repassa `Authorization: Bearer` para o NestJS — ver `app/api/auth/*` como referência.
2. Server Component usando `cookies()` de `next/headers` para ler a sessão e buscar dados diretamente no servidor.

`middleware.ts` protege toda rota exceto `/login`. `AuthProvider`/`useAuth()` (`components/auth/`) expõe o usuário autenticado e `logout()` para componentes cliente.

⚠️ Pendência conhecida: `/ingredients` (Etapa 8) ainda usa o padrão antigo de colar token manualmente — não foi migrada para o padrão de cookie. Migrar quando essa tela for reconstruída.

## 6. Regras de banco

- Chave primária: UUID em todas as tabelas de domínio de negócio.
- Toda mudança de schema ocorre via migration versionada — nunca alteração manual direta no banco.
- Constraints de integridade referencial obrigatórias entre `Organization`/`BusinessUnit` e as demais entidades.
- Tabelas de histórico (versões de ficha técnica, movimentações de estoque, auditoria) não permitem `UPDATE`/`DELETE` de registros já confirmados — apenas inserção de novos registros.
- Todo ambiente de desenvolvimento deve ter seed mínimo reprodutível.

## 7. Regras de auditoria

- Mecanismo central e reutilizável, implementado antes dos módulos de negócio (Etapa 7).
- Toda ação crítica registra: usuário, data/hora, ação, entidade, ID da entidade, valor anterior, valor posterior e metadata relevante (RF-033).
- São consideradas ações críticas, no mínimo, alterações que impactem: estoque, custo, preço, ficha técnica, financeiro e permissões (Seção 9 do Documento Mestre).
- Auditoria serve tanto para segurança quanto para diagnóstico operacional (ex.: investigar por que a margem de um produto caiu).

## 8. Estratégia de testes

- Testes de cálculo com valores conhecidos são obrigatórios para os módulos de custo, ficha técnica, financeiro e BI — sempre demonstrando o exemplo numérico esperado vs. obtido.
- Cada módulo de negócio inclui testes de autorização (permissão negada deve bloquear a operação).
- Teste ponta a ponta obrigatório antes de avançar de fase: ciclo completo Fornecedor → Compra → Ingrediente → Estoque → Produto → Ficha Técnica → Venda → Baixa de estoque → Financeiro. A fase só é considerada concluída se os números baterem.
- Framework de testes: a definir na Etapa 3, junto da stack.

## 9. Regras para migrations

- Toda entidade nova ou alterada gera migration própria, versionada e reversível quando possível.
- Migrations devem ser executadas e validadas (rodar + testar) antes de uma etapa ser considerada concluída.
- Seed de desenvolvimento deve acompanhar novas entidades relevantes.

## 10. Regra de não implementar funcionalidades futuras sem solicitação

O agente implementa **apenas o escopo da etapa solicitada**. Não adianta funcionalidades de etapas futuras do roadmap, mesmo que pareçam relacionadas ou "quase prontas". Ao final de cada etapa, o agente para e aguarda a próxima instrução explícita — nunca prossegue automaticamente para a etapa seguinte.

## 11. Regra de nunca alterar uma regra de negócio sem sinalizar

Nenhuma regra de negócio (`BR-XXX`) ou requisito (`RF-XXX`) do Documento Mestre pode ser alterado, reinterpretado ou "resolvido temporariamente" pelo agente. Decisões ainda pendentes (`PD-001` a `PD-008`, listadas em `docs/pendencias.md`) devem ser sinalizadas ao usuário — nunca resolvidas unilateralmente, mesmo que a implementação pareça exigir uma resposta imediata.

## 12. Regra de manter documentação e código sincronizados

Qualquer decisão tomada durante a implementação (resolução de uma pendência, escolha técnica relevante, mudança de escopo) deve ser registrada de volta neste `CLAUDE.md` e, quando for uma decisão de negócio, também deve ser sinalizada para eventual atualização do Documento Mestre. O código nunca deve divergir silenciosamente da documentação.

## 13. Histórico de decisões registradas

| Data | Decisão | Etapa |
|------|---------|-------|
| 2026-08-17 | Leitura da documentação validada pelo usuário | Etapa 1 |
| 2026-08-17 | CLAUDE.md criado | Etapa 2 |
| 2026-08-17 | Stack técnica confirmada: Next.js+TS (front), NestJS+TS (back), PostgreSQL, Prisma, Jest, Playwright, Docker | Etapa 3 |
| 2026-08-17 | Fundação organizacional criada: Organization/BusinessUnit (UUID, FK RESTRICT, unique [organizationId, name]), migration versionada, seed mínimo. `prisma generate`/`migrate dev` bloqueados no sandbox de desenvolvimento (binário do engine); migration validada via SQL direto + testes automatizados com driver `pg` (7/7 passando) | Etapa 5 |
| 2026-08-17 | Autenticação e autorização implementadas: User/Role/Permission/RolePermission/UserRole (migration versionada); JWT stateless (login/me) — logout é decisão técnica de escopo: sem blacklist/refresh-token nesta etapa (revisar se necessidade de revogação imediata surgir); PermissionsGuard central reutilizável (BR-014); perfis seed ADMIN/STOCK_OPERATOR/FINANCE/VIEW_ONLY, só ADMIN com permissões nesta etapa (módulos de negócio ainda não existem). Resolvida dependência circular AuthModule↔UsersModule via forwardRef. Mesma limitação de `prisma generate` da Etapa 5 impede subir a app completa no sandbox — validado via 11 testes unitários (guard/service com mocks) + 11 testes estruturais via SQL direto (`pg`) | Etapa 6 |
| 2026-08-17 | Auditoria central implementada: AuditLog (migration versionada) com append-only garantido por trigger no PostgreSQL (bloqueia UPDATE/DELETE mesmo fora da aplicação — validado por teste); AuditService global (`record`, `findByEntity`, `findMany`) pronto para ser reutilizado pelos módulos de negócio nas próximas etapas, sem integração retroativa em Users/Auth ainda (fora do escopo desta etapa). `entity`/`action` como strings livres (não enum) para não acoplar o mecanismo central a módulos futuros. 14 testes unitários + 6 estruturais (incl. bloqueio de UPDATE/DELETE) passando | Etapa 7 |
| 2026-08-17 | Ingredientes implementado (RF-009, RF-010, BR-003): catálogo no nível da Organization (não por BusinessUnit — receitas normalmente compartilhadas entre unidades; revisar se necessário catálogo por unidade). Campo `currentStock` de RF-009 deliberadamente omitido — pertence ao módulo de Estoque (Etapa 13); só `minimumStock` (limite configurado) existe agora. `standardUnit` é texto validado por lista prática (kg/g/l/ml/un), sem resolver PD-011 (conversão de unidades, segue em aberto). Permissões `ingredients:read`/`ingredients:manage` concedidas só a ADMIN nesta etapa — RF-030 restringe acesso de STOCK_OPERATOR a informação de lucro/custo; revisar granularidade de permissão por perfil quando Estoque/Financeiro existirem. Primeiro módulo de negócio a reutilizar AuditService (Etapa 7) e PermissionsGuard (Etapa 6) sem duplicar mecanismo, validando a arquitetura definida no CLAUDE.md Seção 4. BR-004 (recálculo de produtos ao mudar custo) não implementado — não existe Produto/FichaTecnica ainda; alteração de custo gera nota informativa no audit log até a Etapa 10. 20 testes unitários + 6 estruturais passando; frontend mínimo em `/ingredients` (autenticação via token colado manualmente — não há tela de login ainda) | Etapa 8 |
| 2026-08-17 | Produtos implementado (RF-001/002/003, BR-001): status DRAFT/ACTIVE/INACTIVE (enum nativo Postgres), catálogo por Organization (mesmo critério de Ingredient). BR-001 ("não pode ativar sem ficha técnica válida") resolvido com FichaTecnicaValidationPort injetável — a implementação atual (PendingFichaTecnicaValidator) SEMPRE nega ativação, já que Ficha Técnica não existe (Etapa 10); nenhum produto pode ser ativado até a porta ser trocada por uma implementação real, o que respeita a regra sem inventar exceção temporária. Inativação (RF-003) não depende dessa checagem. Unique (organizationId, name) e (organizationId, internalCode) — NULL em internalCode não conflita. Permissões `products:read`/`products:manage` só ADMIN, mesma lógica da Etapa 8. 26 testes unitários (incl. prova de que BR-001 bloqueia e de que a porta funciona quando aprova) + 8 estruturais passando | Etapa 9 |
| 2026-08-17 | Ficha Técnica implementada (RF-004 a RF-007, BR-001 a BR-005) — núcleo funcional do sistema. Versionada e imutável (nunca UPDATE numa versão — cada mudança cria version+1); só uma versão "corrente" por produto garantida por índice único PARCIAL no Postgres (validado com teste real de conflito). BR-001 finalmente resolvido de verdade: PendingFichaTecnicaValidator (Etapa 9) substituído por FichaTecnicaValidator real, que consulta a ficha corrente do produto — prova de integração ponta a ponta incluída. Conversão de unidade implementada APENAS para família métrica pura (kg↔g, l↔ml, fator 1000) — conversões entre famílias diferentes continuam bloqueadas, preservando PD-011 em aberto. UC-002 (ingrediente inativo não pode entrar em nova composição) e ausência de custo médio são validados na criação. "Custo da embalagem" (RF-006) não é um indicador separado — embalagem é só mais um item da ficha, igual ao exemplo do Documento Mestre; custo indireto (PD-003) não incorporado, totalCost = soma dos itens apenas. BR-004 (recálculo ao mudar custo) resolvido com recálculo SOB DEMANDA (`getCurrentCostSummary`, compara custo congelado vs. custo atual do ingrediente) em vez de reescrever versões históricas ou auto-versionar — preserva a imutabilidade. CMV/markup/margem calculados com fórmulas padrão de gestão (CMV%=custo/preço×100, markup=preço/custo, margem%=(preço-custo)/preço×100); indicadores retornam null quando o produto não tem preço definido (rascunho). Exemplo matemático completo demonstrado em teste (Smash Burger: pão+carne+queijo+molho+embalagem = R$9,10 de custo, 28.90 de venda → CMV 31,49%, markup 3,18x, margem 68,51%, lucro R$19,80). 51 testes unitários + 8 estruturais passando | Etapa 10 |
| 2026-08-17 | Fornecedores implementado (RF-011, RF-012): CRUD completo com inativação (nunca exclusão física), relação N:N Fornecedor↔Ingrediente via SupplierIngredient. "Fornecedor preferencial" (RF-012) — só um por ingrediente — garantido por índice único PARCIAL no Postgres, mesma técnica da "versão corrente" da Etapa 10; lógica de marcar-como-preferencial (desmarcar outros antes de vincular, dentro de transação) testada explicitamente, incluindo ordem das operações. RF-013 (histórico de preços) NÃO implementado nesta etapa — depende de dados reais de Compras (Etapa 12), que ainda não existe; a estrutura relacional criada é o que uma futura Compra vai referenciar. Permissões `suppliers:read`/`suppliers:manage` só ADMIN, mesmo padrão das etapas anteriores. 59 testes unitários + 8 estruturais passando | Etapa 11 |
| 2026-08-17 | Compras implementado (RF-014): registro (DRAFT) e confirmação, com efeitos em Estoque/Financeiro preparados via evento interno `purchase.confirmed` (EventEmitter2), NÃO implementados diretamente — Estoque (Etapa 13) e Financeiro (Etapa 15) ainda não existem. PurchasesModule não conhece nenhum dos dois; qualquer módulo futuro só precisa se inscrever no evento, sem exigir mudança em Compras (claude/CLAUDE.md, Seção 4). Único listener ativo hoje: IngredientsPurchaseListener, que atualiza `lastCost`/`lastPurchaseDate` do ingrediente (dado objetivo) — NUNCA `averageCost`, pois BR-008 (custo médio) depende de PD-002 (metodologia), que segue em aberto; testado explicitamente que `averageCost` nunca aparece no payload de update. Conversão de preço por unidade (kg↔g, l↔ml) reaproveita o mesmo unit-conversion.ts da Ficha Técnica (Etapa 10), agora movido para `src/common/`. Status DRAFT/CONFIRMED/CANCELLED — apenas DRAFT pode ser confirmada ou cancelada (estado terminal depois disso). Compra guarda organizationId + businessUnitId (RF-014 "unidade") e referências a criador/confirmador. Permissões `purchases:read`/`purchases:manage` só ADMIN. 75 testes unitários + 8 estruturais passando | Etapa 12 |
| 2026-08-17 | Estoque implementado (RF-015, RF-017, RF-018, BR-006, BR-010 a BR-012): StockBalance (saldo atual por unidade+ingrediente — o campo `currentStock` de RF-009 que foi deliberadamente omitido de Ingredient na Etapa 8) e StockMovement (histórico append-only, mesma técnica de trigger da audit_logs/Etapa 7). BR-006 finalmente fecha o ciclo aberto na Etapa 12: StockPurchaseListener reage a `purchase.confirmed` gerando entrada de estoque, sem qualquer alteração em PurchasesModule — prova de que o desacoplamento por evento funciona de verdade, com dois listeners independentes reagindo ao mesmo evento. BR-010 (saldo nunca negativo) aplicado como regra geral segura, dentro de transação atômica (movimentação + saldo); ressalva de PD-001 (venda sem estoque) é específica do módulo de Vendas (Etapa 16, ainda não existe) e não foi resolvida aqui. RF-017: motivo obrigatório em ajuste manual (enum LOSS/WASTE/INVENTORY/CORRECTION/RETURN — categorias do próprio Documento Mestre), reforçado por CHECK constraint no banco (testado). RF-018/BR-011: implementada só a DETECÇÃO de ingredientes abaixo do mínimo (query `listBelowMinimum`) — entrega de alerta (e-mail/push) não implementada, pois depende de PD-008 (canais/responsáveis), em aberto. RF-016/BR-009 (baixa por venda) NÃO implementado — depende de Vendas (Etapa 16, PD-010). Mudança de permissões: primeira vez que um perfil além de ADMIN recebe acesso real — `STOCK_OPERATOR` ganha `stock:read`/`stock:manage`, conforme RF-030 descreve literalmente ("registrar entradas; registrar perdas; realizar ajustes autorizados; consultar estoque"); custo continua restrito a ADMIN via ingredients:read/manage. 84 testes unitários + 8 estruturais (incl. bloqueio real de UPDATE/DELETE e CHECK constraints) passando | Etapa 13 |
| 2026-08-17 | Financeiro essencial implementado (RF-020 a RF-024, BR-007, BR-016): FinancialEntry cobre contas a pagar/receber (RF-021/022), categorias de RF-023 (enum explícito, sem invenção), fluxo de caixa (RF-020, soma de entradas/saídas liquidadas num período — granularidade dia/semana/mês é escolha do chamador ao definir o intervalo) e DRE gerencial (RF-024). BR-007 fecha o TERCEIRO braço do evento `purchase.confirmed` (depois de custo, Etapa 12, e estoque, Etapa 13): FinancialPurchaseListener cria lançamento PAYABLE/MATERIA_PRIMA automaticamente, sem alterar PurchasesModule — três listeners independentes provam a arquitetura de desacoplamento por evento. DRE: "taxas" = despesas categoria PLATAFORMA (ex. comissão iFood); "impostos" é parâmetro manual opcional, default zero — PD-006 (regime tributário) segue sem definição, nenhum cálculo automático implementado; CMV aproximado por despesas categoria MATERIA_PRIMA no período (BR-016: derivado de lançamentos registrados/categorizados), já que não há Vendas (Etapa 16) para CMV real por volume vendido — interpretação documentada, revisar quando Vendas existir. FinancialEntry NÃO é append-only (diferente de AuditLog/StockMovement) — tem ciclo de vida editável até liquidação (PENDING→PAID/CANCELLED). Segunda mudança de permissão além de ADMIN: `FINANCE` ganha `financial:read`/`financial:manage`, conforme RF-031 literalmente. Exemplo numérico completo testado (receita R$10.000, taxas R$1.200, CMV R$3.000, despesas operacionais R$2.700 → resultado operacional R$2.500). 97 testes unitários + 7 estruturais passando | Etapa 14 |
| 2026-08-17 | BI e Analytics essencial implementado (RF-025 a RF-027, BR-017) — módulo puramente de leitura/agregação, SEM nova tabela/migration. RF-025: faturamento e CMV/margem/lucro vêm do Financeiro (Etapa 14); "produtos mais lucrativos" ranqueia por lucro ATUAL recalculado ao vivo via FichaTecnicaService.getCurrentCostSummary (Etapa 10) — satisfaz BR-017 (indicadores devem refletir custo atual) reutilizando código já testado, sem duplicar cálculo. "Produtos mais vendidos", "ticket médio" e "ponto de equilíbrio" retornam null com nota explicativa — dependem de volume de vendas (Vendas, Etapa 16, PD-010); ponto de equilíbrio depende também de uma classificação custo fixo/variável que o Documento Mestre não define, então não foi inventada. RF-026: dashboard de estoque inteiramente composto a partir do StockService (Etapa 13) — novo método `getConsumptionSummary` (soma de saídas por ingrediente num período) adicionado ao StockService, não ao Analytics, mantendo a lógica de domínio centralizada; hoje só reflete ajustes manuais, já que baixa por venda ainda não existe. RF-027: histórico de preço por fornecedor derivado de PurchaseItem+Purchase (Etapa 12), fornecedores vinculados via SuppliersService.findSuppliersByIngredient (Etapa 11) reutilizado sem duplicação. Permissões: reaproveitadas as já existentes por área (`financial:read` para dashboard executivo, `stock:read` para estoque, `suppliers:read` para análise de fornecedores) em vez de criar uma permissão nova de "analytics" — mais coerente semanticamente. 107 testes unitários passando (nenhum teste estrutural novo, já que não há schema novo) | Etapa 15 |
| 2026-08-17 | Vendas implementado (RF-016, BR-009) — escopo confirmado com o usuário antes de codar (PD-010, resolvida: venda de produto único + desconto simples opcional, ver pendencias.md). PD-001 também resolvida nesta etapa: venda NUNCA bloqueia por falta de estoque, apenas sinaliza (`hadInsufficientStock` no registro + `stockWarnings` na resposta/auditoria) — implementado via novo parâmetro `allowNegative` em `StockService.applyMovement`; Compras e ajustes manuais continuam bloqueando saldo negativo (BR-010 inalterado para esses casos). Consumo de estoque (RF-016/BR-009) é SÍNCRONO e transacional dentro de `SalesService.registerSale` — diferente do padrão de evento usado em Compras, porque UC-004 descreve a baixa como parte íntegra do mesmo caso de uso, não um efeito futuro a preparar; refatorei `StockService` para expor `applyMovementInTransaction` (recebe um client de transação externo), permitindo que Venda + todas as movimentações de estoque aconteçam atomicamente numa única transação, sem duplicar a lógica de cálculo de saldo/BR-010. Lançamento financeiro (receita) continua via evento `sale.registered` — mesmo padrão desacoplado de `purchase.confirmed` (Etapas 12-14) — `SalesFinancialListener` cria lançamento RECEIVABLE/VENDAS; categoria `VENDAS` adicionada ao enum `FinancialCategory` (RF-023 só definia categorias do lado de despesa, receita de venda precisava de categoria própria). `MovementSource.SALE` adicionado (ponto de extensão já reservado desde a Etapa 13); CHECK constraint consolidado agora exige referência de origem tanto para PURCHASE quanto para SALE. Produto só pode ser vendido se `status=ACTIVE` (reutiliza BR-001 — produto ativo já garante ficha técnica válida). Este módulo também desbloqueia parcialmente os indicadores da Etapa 15 que dependiam de Vendas ("produtos mais vendidos"/"ticket médio" agora têm dados reais disponíveis, mas o cálculo desses indicadores específicos ainda não foi implementado no Analytics — outra oportunidade futura). 118 testes unitários (11 novos) + 9 estruturais passando | Etapa 16 |
| 2026-08-17 | Simulações e Projeções implementado (RF-008) — SEM nova tabela/migration, reutilização direta das funções puras de cálculo da Ficha Técnica (Etapa 10: calculateItemCost/calculateTotals), sem duplicar lógica. Novo endpoint `POST /products/:id/ficha-tecnica/simulate` (permissão `ficha_tecnica:read`, já que é só uma prévia — não escreve nada). Cobre os 4 exemplos citados pelo RF-008 num único mecanismo genérico: `costOverride` por item simula "trocar fornecedor" (preço hipotético, sem alterar Ingredient.averageCost); `quantity` livre simula "aumentar gramatura"; `salePriceOverride` simula "alterar preço" ou "conceder desconto" (mostra só o impacto no cálculo — NÃO implementa política de aprovação/limite de desconto, que é PD-007, ainda em aberto). Compara automaticamente com a versão corrente da ficha técnica quando existe (totalCostDelta/estimatedProfitDelta). Não gera auditoria — simulação não é "ação crítica" (RF-033), já que nada muda de estado. Testado explicitamente que a simulação não chama create/update em nenhuma tabela. 8 novos testes unitários incluindo os 4 exemplos numéricos completos do RF-008 (126 testes totais) | Etapa 17 |
| 2026-08-19 | **Correção pós-validação local (primeira execução real fora do sandbox):** a migration `20260817220000_sales` falhava com erro `55P04 unsafe use of new value "SALE"` ao rodar `npx prisma migrate deploy`. Causa: PostgreSQL não permite usar um valor de enum recém-criado (`ALTER TYPE ... ADD VALUE`) na mesma transação em que foi adicionado; o Prisma aplica cada arquivo de migration dentro de uma única transação, mas os testes no sandbox (via `psql -f`, sem transação explícita) nunca teriam detectado isso. Corrigido dividindo a migration em duas: `20260817220000_sales` (só os `ALTER TYPE ADD VALUE`) e `20260817220001_sales_tables` (tudo que usa os novos valores). Validado simulando o comportamento transacional do Prisma (`BEGIN`/`COMMIT` por arquivo) contra Postgres real — todas as 12 migrations aplicam sem erro. Esse é exatamente o tipo de bug que só aparece rodando a aplicação de verdade, fora do sandbox; nenhuma outra migration tinha esse padrão (`ADD VALUE` seguido de uso no mesmo arquivo). | Correção |
| 2026-08-19 | **Segunda correção pós-validação local:** `npm run start:dev` falhava com 16 erros de tipo TS2769/TS2339 em `ficha-tecnica.service.ts`, `purchases.service.ts`, `sales.service.ts`, `stock.service.ts` e `suppliers.service.ts`. Causa: todos os usos de `this.prisma.$transaction(async (tx: typeof this.prisma) => ...)` tipavam o cliente de transação incorretamente como `PrismaService` inteiro (que inclui `$connect`/`$disconnect`/`onModuleInit`, ausentes no cliente de transação real do Prisma). No sandbox de desenvolvimento, como o Prisma Client nunca foi gerado de verdade (mesma limitação de rede documentada desde a Etapa 5), esses arquivos usavam um client "stub" totalmente `any`, então o erro de tipo nunca apareceu em nenhuma validação feita aqui — só surgiu na primeira compilação real do usuário, fora do sandbox. Corrigido trocando a anotação para `Prisma.TransactionClient` (tipo oficial do Prisma para callbacks de `$transaction` interativo) em todos os 6 pontos, incluindo a assinatura pública de `StockService.applyMovementInTransaction` (usada por SalesService para compor a venda + movimentações de estoque numa única transação, Etapa 16). | Correção |
| 2026-08-19 | **Terceira correção — encontrada testando o fluxo completo manualmente pela primeira vez (Ingrediente → Produto → Ficha Técnica → Fornecedor → Compra):** `POST /purchases` retornava 500 com `PrismaClientValidationError: Invalid value for argument purchaseDate: premature end of input. Expected ISO-8601 DateTime`. Causa: o DTO valida `purchaseDate` com `@IsDateString()` (aceita datas sem componente de hora, ex. `"2026-08-19"`), mas o Prisma exige um `DateTime` completo — o valor estava sendo passado cru (string) direto para `prisma.purchase.create()` sem conversão via `new Date(...)`. Corrigido em `purchases.service.ts` (`purchaseDate`). Varredura no restante do codebase encontrou o MESMO padrão em mais 2 lugares que nunca tinham sido exercitados manualmente: `financial.service.ts` (`dueDate`, em `create` e `update`) e `products.service.ts` (`promotionalPeriodStart`/`promotionalPeriodEnd`, em `create` e `update`) — todos corrigidos com a mesma conversão condicional (`dto.campo ? new Date(dto.campo) : undefined`). `sales.service.ts` (`saleDate`) já estava correto desde a Etapa 16. Nenhum teste unitário pegou isso porque os mocks de Prisma não validam tipo de argumento como o client real faz — reforça a importância de testar o fluxo real manualmente antes de considerar qualquer etapa "pronta para uso". | Correção |
| 2026-08-19 | **Frontend — Design System v1 implementado.** Usuário forneceu briefing próprio de direção visual (Linear + Stripe Dashboard + shadcn, referência do próprio logo/paleta da marca) e uma imagem do logo oficial; paleta convertida do logo com precisão (hex→HSL). Stack: Next.js + Tailwind v3.4.19 (fixado deliberadamente — `npm install` sem pin puxa v4, incompatível com o ecossistema shadcn/Radix usado) + shadcn/ui (componentes escritos à mão, sem depender do CLI) + Lucide + Inter. Construídos: tokens de cor/tipografia (`app/globals.css`, `tailwind.config.ts`), 5 primitivos shadcn (`Button`, `Badge`, `Card`, `Table`, `Dialog`), os 10 componentes de domínio pedidos pelo usuário (`KpiCard`, `StatusBadge`, `DataTable`, `MoneyValue`, `PercentageChange`, `StockStatus`, `EmptyState`, `ConfirmDialog`, `PageHeader`, `AuditTimeline`), `Sidebar`/`AppShell` com a estrutura de navegação exata aprovada (Dashboard/Operação/Gestão/Sistema), e uma página `/design-system` reunindo tudo para validação visual. Antes de escrever código, validei a direção com um mockup renderizado via Visualizer (paleta real aplicada em sidebar+KPIs+alertas) e obtive aprovação explícita do usuário. `npm run build`/`tsc --noEmit`/lint 100% limpos — testei contornando temporariamente só a fonte Inter (bloqueio de rede do sandbox a `fonts.googleapis.com`, mesma classe de limitação do Prisma/Playwright), depois restaurada antes do commit. Nenhuma tela real (Dashboard/Produtos/etc.) foi implementada ainda — só a fundação reutilizável, por decisão explícita do usuário ("montar a base do design system inteiro antes de qualquer tela"). | Design System |
| 2026-08-19 | **Frontend — Login implementado, primeira tela real.** Decisão de arquitetura de autenticação: token JWT guardado em cookie `httpOnly` (`wb_session`, gerenciado por Route Handlers Next.js em `app/api/auth/{login,logout,me}`), NUNCA em `localStorage` nem exposto ao JavaScript do navegador — reduz superfície de roubo de token via XSS. `middleware.ts` protege toda rota exceto `/login`, redirecionando para login (com `?from=` para retorno) quando não há cookie de sessão. `AuthProvider` (contexto React) busca o usuário via `/api/auth/me` e expõe `logout()`; `Sidebar` agora mostra nome do usuário logado + botão de sair. Criada página `/dashboard` como placeholder mínimo (só o suficiente para o login ter destino real) — o dashboard executivo completo é escopo de uma etapa futura separada. **Decisão de padrão para TODAS as próximas telas:** como o token fica inacessível ao JS por design, páginas que buscam dados reais do backend NÃO podem fazer `fetch` direto do cliente com o token — devem seguir o mesmo padrão de proxy (Route Handler lê o cookie no servidor e repassa `Authorization: Bearer` para o NestJS) ou usar Server Components com `cookies()` de `next/headers`. A página `/ingredients` (Etapa 8, token colado manualmente) ainda NÃO foi migrada para esse padrão — inconsistência conhecida, sinalizada para quando essa tela for reconstruída. `npm run build`/`tsc --noEmit`/lint 100% limpos (mesmo contorno temporário de fonte do commit anterior). 2 primitivos shadcn novos (`Input`, `Label`). | Login |
| 2026-08-19 | **Frontend — Dashboard executivo real implementado.** Substitui o placeholder mínimo criado junto com o Login. Consome `GET /analytics/executive` (Etapa 15) e `GET /stock/below-minimum` (Etapa 13), últimos 30 dias, via o novo helper reutilizável `lib/api-proxy.ts` (`proxyBackendGet`) — extraído nesta etapa exatamente para não repetir a lógica de ler cookie/repassar Bearer em cada Route Handler novo; toda tela futura que só precisa LER dado deve usar esse helper. KPIs mostrados: Faturamento, CMV, Margem bruta, Resultado operacional — só os 4 indicadores que o backend realmente calcula; NÃO forcei um card de "ticket médio" com dado falso só para preencher espaço, mostrei a nota `indicadoresNaoDisponiveis` do próprio backend explicando a ausência (produtos mais vendidos/ticket médio/ponto de equilíbrio dependem de Vendas real, Etapa 16, ainda sem volume suficiente). Seção "Atenção" busca estoque crítico com tolerância a falha de permissão (perfil sem `stock:read`, ex. FINANCE, não derruba o resto do dashboard — só omite essa seção). "Produtos mais lucrativos" renderiza o ranking ao vivo (BR-017) já construído na Etapa 15. `KpiCard.value` teve o tipo ampliado de `string` para `ReactNode` para aceitar `<MoneyValue />` diretamente, em vez de formatar valor monetário manualmente na tela (evita duplicar lógica de formatação). `npm run build`/`tsc --noEmit`/lint 100% limpos (mesmo contorno de fonte Inter, restaurado antes do commit). | Dashboard |
| 2026-08-19 | **Frontend — Produtos (lista + detalhe com tabs) implementado.** Lista em formato de tabela (Produto/Custo/Venda/CMV/Margem/Status), não cards com foto, conforme o brief. Custo/CMV/margem por produto vêm da ficha técnica corrente de cada um (1 chamada extra por produto na lista — aceitável para o volume de produtos do MVP). Detalhe com as 4 tabs exatas do brief (Visão geral, Ficha técnica, Custos, Histórico): Visão geral mostra os 4 indicadores + um card de alerta quando `costDrifted=true` (custo do ingrediente mudou desde a última versão da ficha, usando a comparação frozen-vs-live da Etapa 10 — mais simples que o "qual ingrediente mudou e qual o impacto por unidade" do brief original, que exigiria um endpoint novo de auditoria por entidade que ainda não existe no backend; documentado como possível evolução). Ficha técnica replica o formato de "quase planilha" pedido (ingrediente/quantidade/unidade/custo + resumo). Custos compara lado a lado o congelado na versão vs. com preço atual dos ingredientes. Histórico lista as versões da ficha técnica (RF-007) — não a auditoria genérica (`AuditService.findByEntity` existe no backend mas não está exposta por nenhum controller ainda). Botão "Novo produto" removido da lista — o pedido era explicitamente lista+detalhe, não o formulário de criação; ficaria um link morto. Novo primitivo shadcn: `Tabs` (Radix). 5 novos Route Handlers dinâmicos (`/api/products/[id]/**`) usando o helper `proxyBackendGet`. `npm run build`/`tsc --noEmit`/lint 100% limpos (1 erro real de lint corrigido: aspas retas não escapadas em JSX). | Produtos |
| 2026-08-19 | **Frontend — editor de composição da Ficha Técnica implementado (`/products/[id]/ficha-tecnica/edit`), primeira tela que ESCREVE dado.** Reutiliza o endpoint de simulação RF-008 (Etapa 17) para o "resumo atualiza imediatamente" pedido no brief — debounce de 400ms a cada mudança de linha, sem persistir nada até o usuário clicar "Salvar nova versão" (que aí sim chama o `POST` real de criação de versão, Etapa 10). Isso evita reimplementar a matemática de custo/CMV/margem em JavaScript — o mesmo motor de cálculo do backend é a única fonte da verdade, inclusive no preview. `lib/api-proxy.ts` ganhou `proxyBackendMutation` (POST/PATCH/DELETE), generalizando o padrão de proxy também para escritas — não só leituras como até agora; a validação de negócio continua só no NestJS (DTOs + class-validator), o proxy não duplica validação. Formulário carrega a composição atual (se existir) para edição incremental, e só lista ingredientes ATIVOS no seletor (ingrediente inativo não pode entrar em nova composição, UC-002 — reforçado também no backend). Novo primitivo `Select` (nativo estilizado, não Radix Select completo — decisão de escopo enxuto para o MVP). Link "Editar composição"/"Criar ficha técnica" adicionado na tab Ficha Técnica da página de detalhe do produto. `npm run build`/`tsc --noEmit`/lint 100% limpos. | Ficha Técnica (editor) |
| 2026-08-19 | **Correção — item "Fichas técnicas" da sidebar apontava para `/ficha-tecnica`, rota que nunca existiu** (404). Ficha técnica sempre pertence a um produto específico — não há conceito de "lista geral de fichas técnicas" independente de Produto no backend. Removido o item duplicado da sidebar (apontar ambos "Produtos" e "Fichas técnicas" para `/products` faria os dois ficarem destacados simultaneamente, confuso); a entrada real para trabalhar com ficha técnica continua sendo `/products` → produto → tab "Ficha técnica". Encontrado pelo usuário testando a navegação real pela sidebar, não pelos links internos das páginas. | Correção |
| 2026-08-19 | **Frontend — Ingredientes migrado do token manual para o cookie de sessão (pendência sinalizada desde a Etapa de Login).** Página antiga (Etapa 8, estilo inline, token JWT colado à mão num campo de texto) substituída por uma tela real usando o design system e o padrão de proxy estabelecido. Mesmo conjunto de funcionalidades da página antiga (listar, criar, ativar/inativar) mas agora com `ConfirmDialog` antes de inativar (ação com impacto real — ingrediente inativo não pode entrar em nova ficha técnica, UC-002) em vez de ação direta sem confirmação. 3 novos Route Handlers (`POST /api/ingredients`, `PATCH /api/ingredients/[id]/activate`, `PATCH /api/ingredients/[id]/deactivate`) via `proxyBackendMutation`. Tipo `Ingredient` no frontend expandido com todos os campos do backend (`storageLocation`, `minimumStock`, `lastCost`, `lastPurchaseDate`) para já cobrir a estrutura completa. `npm run build`/`tsc --noEmit`/lint 100% limpos. | Ingredientes |
| 2026-08-19 | **Frontend — Estoque implementado (RF-017, RF-018/BR-011).** Tabela de saldos usando o `StockStatus` (3 estados: normal/atenção/crítico) já construído na Etapa de Design System, sem pintar linha inteira, conforme o brief. Formulário de ajuste manual (entrada/saída, motivo obrigatório — reforçado tanto no DTO do NestJS quanto por CHECK constraint no banco desde a Etapa 13) inline, mesmo padrão de "toggle abre formulário" já usado em Ingredientes. Seção "Movimentações recentes" enriquece o nome do ingrediente cruzando com a lista de ingredientes já carregada no cliente — o endpoint `GET /stock/movements` do backend não inclui a relação `ingredient` (só retorna `ingredientId`), então evitei um join redundante no backend e resolvi no frontend com o dado que já estava disponível. 3 novos Route Handlers (`GET /api/stock/balances`, `POST /api/stock/adjustments`, `GET /api/stock/movements`). `npm run build`/`tsc --noEmit`/lint 100% limpos. | Estoque |
| 2026-08-19 | **Frontend — Fornecedores implementado (RF-011, RF-012).** Lista + detalhe, mesmo padrão de Ingredientes/Produtos. Detalhe mostra os dados de contato e a seção "Ingredientes fornecidos" (RF-012 — vínculo N:N Fornecedor↔Ingrediente da Etapa 11): vincular, desvincular (com `ConfirmDialog`, já que remove um vínculo real) e "Marcar como preferencial" — reaproveita o mesmo endpoint de vínculo (`POST .../ingredients`) que o backend já usa para upsert + desmarcar qualquer outro preferencial do mesmo ingrediente numa transação (Etapa 11), sem nenhuma lógica nova no frontend além de chamar com `isPreferred: true`. Formulário de vínculo só lista ingredientes ainda não vinculados a este fornecedor (filtro no cliente, cruzando a lista de ingredientes ativos com os vínculos já existentes). 6 novos Route Handlers, incluindo a primeira rota `DELETE` do frontend (`/api/suppliers/[id]/ingredients/[ingredientId]`, dois parâmetros dinâmicos). `npm run build`/`tsc --noEmit`/lint 100% limpos — total de 25 rotas de API no projeto até aqui. | Fornecedores |
| 2026-08-19 | **Frontend — Compras implementado (RF-014, registro + confirmação).** Lista (fornecedor/data/itens/total/status, cruzando nome do fornecedor no cliente já que `Purchase` só guarda `supplierId`) + formulário de nova compra (mesmo padrão "quase planilha" do editor de Ficha Técnica: linhas de ingrediente/quantidade/unidade/preço unitário, total calculado ao vivo no cliente — aqui não há endpoint de simulação como no RF-008, então o total é só uma soma simples, sem chamar o backend a cada tecla) + página de detalhe com os botões "Confirmar"/"Cancelar", só visíveis quando `status=DRAFT` (compra confirmada ou cancelada é estado terminal, Etapa 12). O texto de aviso antes de confirmar explica os três efeitos automáticos que a confirmação dispara (estoque, custo do ingrediente, conta a pagar) — a mesma cadeia de eventos que testamos via terminal nas Etapas 12-14, agora visível e acionável pela interface, fechando o primeiro fluxo operacional completo sem depender de PowerShell. 4 novos Route Handlers. `npm run build`/`tsc --noEmit`/lint 100% limpos. | Compras |
| 2026-08-19 | **Frontend — Vendas implementado (RF-016/BR-009, PD-001 visível na interface).** Lista + formulário de nova venda (produto ativo, quantidade, desconto opcional percentual/fixo). O ponto central desta tela: PD-001 (resolvido na Etapa 16 do backend — "venda nunca bloqueia por falta de estoque, só sinaliza") agora tem representação visual real — depois de registrar, se `hadInsufficientStock=true`, aparece um card de aviso (borda âmbar, ícone de alerta) listando exatamente quais ingredientes ficaram com saldo negativo e qual o saldo resultante, usando o array `stockWarnings` que o backend já retornava desde aquela etapa mas que nunca tinha sido consumido por nenhuma tela até agora. Prévia de valor bruto calculada no cliente antes de enviar (`preço × quantidade`, sem chamar o backend — diferente da Ficha Técnica, aqui não existe endpoint de simulação de venda). Sidebar ganhou o item "Vendas" (não existia na estrutura de navegação original aprovada — o briefing é anterior à Etapa 16 do backend, que introduziu o módulo). 2 novos Route Handlers. `npm run build`/`tsc --noEmit`/lint 100% limpos (1 warning de dependência de `useEffect` corrigido). Com isso, o ciclo operacional completo (ingrediente → fornecedor → compra → estoque → produto → ficha técnica → venda → financeiro) é operável 100% pela interface, sem depender de terminal. | Vendas |
| 2026-08-19 | **Frontend — Financeiro implementado (RF-020 a RF-024), décima e última tela do plano original de 10 telas do briefing de design.** Página única com 4 tabs (Fluxo de caixa, Contas a pagar, Contas a receber, DRE) + um seletor de período (data inicial/final, default últimos 30 dias) compartilhado entre as 3 chamadas de API que dependem de intervalo. DRE renderizado na estrutura contábil clássica pedida no brief (Receita bruta → (-) taxas → (-) impostos → (-) CMV → Lucro bruto → (-) despesas operacionais → Resultado operacional), consumindo exatamente os campos que `FinancialService.getDre` já calculava desde a Etapa 14 — nenhuma lógica de cálculo nova no frontend. Contas a pagar/receber reaproveitam o `DataTable` genérico com uma ação "Marcar como pago" por linha (só visível quando `status=PENDING`), com `ConfirmDialog` antes de liquidar. Formulário de lançamento manual só oferece as 8 categorias de despesa de RF-023 (`FinancialCategoryDto` do backend não inclui `VENDAS` na validação de criação manual — corretamente, já que `VENDAS` é reservada para os lançamentos automáticos gerados pelo listener de vendas desde a Etapa 16; não é uma lacuna, é a mesma decisão de design do backend refletida no formulário). 5 novos Route Handlers. `npm run build`/`tsc --noEmit`/lint 100% limpos. **Com esta tela, as 10 telas planejadas no briefing de design (19/08/2026) estão implementadas e validadas em produção local pelo usuário: Login, Dashboard, Produtos, Ficha Técnica, Ingredientes, Estoque, Fornecedores, Compras, Vendas, Financeiro.** | Financeiro |
| 2026-08-19 | **Frontend — Correção: formulário de criação de produto (`/products/new`) implementado.** Lacuna real identificada pelo usuário: a lista de Produtos (Etapa "Produtos") tinha o botão "Novo produto" removido deliberadamente porque a página não existia ainda ("o pedido era explicitamente lista+detalhe, não o formulário de criação") — mas sem essa tela, não havia como cadastrar nenhum produto novo pela interface, o que inviabiliza o uso real do sistema. Corrigido: novo `POST /api/products` (Route Handler que faltava), formulário com os campos essenciais (nome, código interno, categoria, descrição, preço de venda, peso final, tempo de preparo — deliberadamente sem imagem/preço promocional/disponibilidade nesta primeira versão, para manter o formulário focado). Após criar, redireciona automaticamente para o editor de ficha técnica do produto recém-criado, seguindo literalmente o UC-001 do Documento Mestre ("sistema permite prosseguir para ficha técnica") — o produto nasce DRAFT e não pode ser ativado sem ficha técnica válida (BR-001), então montar a composição na sequência é o fluxo natural, não uma escolha arbitrária de UX. Botão "Novo produto" restaurado na lista. `npm run build`/`tsc --noEmit`/lint 100% limpos. | Correção |
| 2026-08-19 | **Frontend — BI/Indicadores implementado (RF-026/RF-027), última lacuna da sidebar preenchida.** O item já existia na navegação desde a Etapa de Design System (`href: '/analytics'`) mas nunca teve página construída. Dois endpoints do backend (Etapa 15) nunca tinham sido consumidos por nenhuma tela até agora: `GET /analytics/stock` (consumo de ingredientes no período — distinto do que a tela Estoque já mostra, que é saldo atual e movimentações, não consumo agregado) e `GET /analytics/suppliers/:ingredientId` (análise de preço por fornecedor — histórico de compras, variação min/max/média, fornecedores vinculados com indicação de preferencial). Conteúdo deliberadamente não-redundante com Dashboard/Estoque, já que essas telas já cobrem faturamento/CMV/estoque crítico. 2 novos Route Handlers. `npm run build`/`tsc --noEmit`/lint 100% limpos. **Também produzida nesta sessão:** apresentação de negócio (`Wave-Burger-Apresentacao-de-Negocio.pptx`, 13 slides, PowerPoint) para uso com sócios/compradores — paleta extraída do logo real, conteúdo equilibrado entre visão de negócio e operação prática, todo número citado é real e verificável (126+ testes confirmados rodando a suíte, exemplo de cálculo do Smash Burger claramente rotulado como ilustrativo, nenhuma estatística de mercado ou financeira inventada). | BI/Indicadores + Apresentação |
